/* ============================================================
   Configuração do formulário de cadastro
   ------------------------------------------------------------
   Cole abaixo a URL do seu Web App do Google Apps Script para
   salvar os cadastros direto numa planilha do Google Sheets.

   Passo a passo: docs/google-sheets/README.md

   Enquanto estiver vazio (""), o formulário envia os dados
   pelo WhatsApp automaticamente — então nunca fica "morto".
   ============================================================ */
window.CADASTRO_ENDPOINT = "";

/* WhatsApp usado no fallback (apenas dígitos, com DDI 55). */
window.CADASTRO_WHATSAPP = "5569999688625";
